version: "3.9"

services:
  monitoring:
    build: .
    container_name: cloudforge-monitoring

    ports:
      - "8080:8080"

    restart: unless-stopped

    environment:
      APP_ENV: production
      PORT: 8080
